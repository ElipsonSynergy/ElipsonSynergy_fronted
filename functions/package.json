{
  "name": "functions",
  "type": "module",
  "main": "dist/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "prebuild": "npm --prefix .. run build && npm --prefix .. run build:ssr",
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "echo \"(opcional) usa eslint si quieres\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
     "rimraf": "^6.0.1"
  }
}
