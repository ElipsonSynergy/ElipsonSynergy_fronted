---
// producto.astro - Product page
import BaseLayout from '../layouts/BaseLayout.astro';
import ProductFilter from '../components/ProductFilter.astro';
import ProjectCard from '../components/ProjectCard.astro';
import { projects } from '../data/projects';
---

<BaseLayout 
  title="Nuestros Productos - Elipson Synergy" 
  description="Descubre nuestros desarrollos recientes en diferentes sectores. Proyectos de alto impacto que transforman la industria colombiana."
  keywords="productos, proyectos, desarrollos, agro, jóvenes, store, makers, software, hardware"
>
  <!-- Hero Header Section -->
  <section class="relative py-20 overflow-hidden">
    <!-- Background with gradient -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50"></div>
    <div class="absolute inset-0 opacity-5">
      <div class="grid grid-cols-8 gap-6 h-full p-4">
        <div class="bg-blue-200 rounded opacity-30"></div>
        <div class="bg-indigo-200 rounded opacity-20"></div>
      </div>
    </div>
    
    <!-- Floating decorative elements -->
    <div class="absolute top-10 left-10 w-20 h-20 bg-blue-200 rounded-full opacity-20 animate-pulse"></div>
    <div class="absolute top-32 right-16 w-32 h-32 bg-indigo-200 rounded-full opacity-20 animate-pulse delay-300"></div>
    <div class="absolute bottom-10 left-1/3 w-16 h-16 bg-purple-200 rounded-full opacity-20 animate-pulse delay-700"></div>
    
    <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="animate-fade-in-up">
        <!-- Badge -->
        <div class="inline-flex items-center px-4 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mb-8">
          <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
          </svg>
          Innovación en Acción
        </div>
        
        <!-- Main title -->
        <h1 class="text-4xl md:text-6xl font-bold mb-6 leading-tight">
          <span class="block text-gray-900">Nuestros</span>
          <span class="block text-gradient mt-2">Desarrollos Recientes</span>
        </h1>
        
        <!-- Subtitle -->
        <p class="max-w-4xl mx-auto text-xl md:text-2xl text-gray-600 leading-relaxed">
          En Elipson nos mantenemos activos desarrollando proyectos de alto impacto 
          que ayudan a innovar y transformar diversos sectores de la industria colombiana.
        </p>
        

      </div>
    </div>
  </section>

  <!-- Projects Section -->
  <section class="py-20 bg-white relative">
    <!-- Decorative elements -->
    <div class="absolute top-0 left-0 w-40 h-40 bg-gradient-to-br from-blue-100 to-transparent rounded-full -translate-x-20 -translate-y-20"></div>
    <div class="absolute bottom-0 right-0 w-60 h-60 bg-gradient-to-tl from-indigo-100 to-transparent rounded-full translate-x-20 translate-y-20"></div>
    
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      
      
      <!-- Product Filter Component -->
      <ProductFilter />
      
      <!-- Projects Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {projects.map((project) => (
          <ProjectCard project={project} />
        ))}
      </div>
      
      <!-- Load More Section -->
      <div class="text-center mt-16">
        <p class="text-gray-600 mb-6">
          ¿Interesado en conocer más sobre nuestros proyectos?
        </p>
        <a 
          href="/contactanos" 
          class="btn-primary inline-flex items-center space-x-2 group"
        >
          <span>Conversemos sobre tu proyecto</span>
          <svg class="w-5 h-5 transition-transform duration-200 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="relative py-20 overflow-hidden">
    <!-- Gradient background -->
    <div class="absolute inset-0 gradient-bg"></div>
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-10 left-10 w-3 h-3 bg-white rounded-full animate-pulse"></div>
      <div class="absolute top-32 right-16 w-2 h-2 bg-white rounded-full animate-pulse delay-500"></div>
      <div class="absolute bottom-20 left-1/3 w-1.5 h-1.5 bg-white rounded-full animate-pulse delay-1000"></div>
    </div>
    
    <!-- Decorative elements -->
    <div class="absolute top-10 left-10 w-32 h-32 bg-white/10 rounded-full"></div>
    <div class="absolute bottom-10 right-10 w-24 h-24 bg-white/10 rounded-full"></div>
    
    
  </section>
</BaseLayout> 